<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-end">
        <a
          class="px-3 py-2 bg-red-500 rounded-md mb-5 inline-block text-sm"
          href="/logout"
          >Logout</a
        >
      </div>

      <h5 class="mb-5">Edit your post</h5>
      <form action="/update/<%=post._id  %>" method="post">
        <textarea
          name="content"
          placeholder="What's on your mid ? "
          class="p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-700 rounded-md outline-none block"
        ></textarea>
        <input
          class="px-3 py-2 w-40 text-sm bg-yellow-500 block rounded-md mt-2"
          type="submit"
          value=" Update post "
        />
      </form>
    </div>
  </body>
</html>
